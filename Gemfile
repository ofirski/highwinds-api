source 'https://rubygems.org'
gemspec

gem "httparty", "0.10.2" # This is done in order to keep the httparty version low so so no need for json upgrade 
                         # Notice that you can use 0.1.0 version if you do not like this limitation
group :development do
  gem "rake", "~> 0.9"
end
